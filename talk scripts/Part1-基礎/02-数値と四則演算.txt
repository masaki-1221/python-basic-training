次は「数値と四則演算」についてです。
Pythonでは数値を扱うことができます。

数値の特徴として、文字列と違ってクォーテーションで囲む必要がありません。

（操作：セルを実行）

このように、数字の3が出力されました。

では、四則演算を見ていきましょう。

まずは足し算です。
プラス記号を使うと、足し算をすることができます。

（操作：セルを実行）

なお、全角文字を使うとプログラムは動きません。
コメントや文字列、つまりクォーテーションの中身は全角でも構いませんが、
それ以外のプログラム部分は基本的に半角で書くと覚えておいてください。

次に引き算です。
引き算はマイナス記号を使います。

次に掛け算です。
掛け算はアスタリスクを使います。

次に割り算です。
割り算はスラッシュを使います。

次に、パーセント記号についてです。
パーセント記号を使うと、割り算の余りを求めることができます。

次に、スラッシュを2つ書く方法です。
これは割り算の商を求める、あるいは小数点以下を切り捨てると考えてください。

ここで、計算をクォーテーションで囲むとどうなるか試してみましょう。

（操作：セルを実行）

このように「3 + 7」がそのまま文字列として表示されます。
数字をクォーテーションで囲むと、文字列として扱われるということを覚えておいてください。

以上が「数値と四則演算」の説明でした。
