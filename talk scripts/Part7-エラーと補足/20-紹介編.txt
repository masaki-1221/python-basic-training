最後に「紹介編」として、知っておくと便利な書き方を紹介します。
ここでは覚える必要はありませんが、見かけたときに分かるようにしておきましょう。

--------------------
■ リスト内包表記
--------------------

リスト内包表記は、リストを簡潔に作成する書き方です。

通常のfor文でリストを作る場合、このように書きます。

（操作：セルを実行）

これをリスト内包表記で書くと、1行になります。

（操作：セルを実行）

書き方は、角括弧の中に「式 for 変数 in イテラブル」と書きます。

条件を付けることもできます。

（操作：セルを実行）

生成AIが作るコードでは、リスト内包表記がよく使われます。
見かけたら「リストを作っているんだな」と理解してください。

--------------------
■ 三項演算子
--------------------

三項演算子は、if-elseを1行で書く方法です。

通常のif-elseで書くと、このようになります。

（操作：セルを実行）

これを三項演算子で書くと、1行になります。

（操作：セルを実行）

書き方は「Trueの場合の値 if 条件 else Falseの場合の値」です。

簡単な条件分岐を1行で書きたい場合に使われます。

--------------------
■ アンパック
--------------------

リストやタプルの要素を、複数の変数に一度に代入することを
アンパックと呼びます。

（操作：セルを実行）

for文と組み合わせて使うこともよくあります。

（操作：セルを実行）

--------------------
■ ラムダ式
--------------------

ラムダ式は、簡単な関数を1行で定義する書き方です。

通常の関数定義だとこのように書きます。

（操作：セルを実行）

これをラムダ式で書くと、1行になります。

（操作：セルを実行）

ラムダ式は、sorted関数のキーなど、
一時的に使う簡単な関数を定義する場合に使われます。

（操作：セルを実行）

--------------------
■ まとめ
--------------------

以上で、Python基礎研修の全内容が終了しました。

今回学んだ内容を理解していれば、
生成AIが作るコードの7割程度は読めるようになっているはずです。

分からない部分があっても、生成AIに質問すれば教えてもらえます。
まずはコードを読む経験を積んでいきましょう。

お疲れ様でした。
