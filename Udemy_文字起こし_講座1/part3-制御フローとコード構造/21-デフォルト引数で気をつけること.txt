はい、それではですね。

デフォルト引数で注意してほしいことをですね。

ちょっとここで説明していこうと思いますで例えばですね。

ファンクションで名前はテストファンクとするんですが、エックスという引数とですね得るという引数を渡してやるんですけども、こちらのAIRに

ですね。

リストのデフォルト引数を渡してやります。

で、こちらの。

得るのですね。

リストに対して。

エックスを入れてこのですね。

AIRのリストを返してやるというファンクションがあるとしますで、例えば私がですね。

123というようなリストを持っていてで、こちらのテストファンクを呼び出す時にですね。

こちらに100を追加したいという風にやる場合には、これで実行してやるとですね。

返り値もAに入れて出力してみるんですが、これで今実行しますねで実行すると右手にですね。

元々あった123のリスト200が最後に付け加えられた状態で返ってきますね。

これはですね。

こちらの123のWHYに入れた変数を渡してやると、このエルが123入ってますので、そのエルのリストの最後にですね。

一番1を足した。

この100がですね。

追加されて、そのリストを返すというようなファンクションですので、これはですね。

123の最後は100という風に出力されるのはお分かりかと思いますで、そのほかにもですね。

例えば私はですね。

コードでですね。

例えば私は再度ですね。

他のところへワインを123と定義してですね。

また、今度はですね。

100ではなく、200というものをですね。

追加しますねで、これで実行してやるとですね。

一番初めのプリントで出力される時はもちろん、100が露わされたリストはで、次にやった時はですね。

200というものを追加したので、このように変えてきますねで、これは別に問題ないんですけども。

ではですね。

ちょっとここでですね。

これちょっとコメントアウトしますね。

で、元のこちらのテストファンクにですね。

例えば100と入れますねでこれでですね。

Aが返ってくるので、これをプリントしてやるとどうなるかなんですけれども、これ実際実行してみるとこれ勿論ですね。

実行すると100だけが入ったリストが返ってくるという風になってます。

要は、こちらのデフォルト引数のからのリストがこちらで入ってるので、それに追加しただけですよね。

なのでこれも問題ないように見えるんですけども、これ例えばですね。

もう一度同じようなことを実行するとしますで、これ一回コピーしてですね。

同様にこれ実行するとどうなるかなんですけども、これ実行するとですね。

100が2個入った状況で返ってくるんですね。

で、これ結構バグに繋がることが多いので、PYTHONではですね。

リストはデフォルト引数で与えるべきではないようなことかですね。

暗黙の了解でありますで、これ何でかいうとですね。

これリストはですね。

参照渡しといったようなことをですね。

以前お話ししたかと思うんですけども、このテストファンクをですね。

一番初めに定義した時点で、PYTHONはこちらのAIRをですね。

空のリストをデフォルト引数として用意するので、たった一回だけですね。

これが生成されるんですね。

要は、空のリストを指しているアドレスAIRは一度だけ生成されますので、こちらでですね。

実行した際には勿論空なんですけども、2度目にこれを実行した際にも同じこちらの一番元のプログラムを呼び込んだ際のリストの先頭の

アドレスを指したままなので、これをですね。

再度実行しても一番初めに実行された時のリストの値を参照した。

このAIRですね。

それにですね。

更に100を追加しているという風になってます。

なのでちょっとですね。

なかなか分かりにくいかと思うんですけども、PYTHONのデフォルト引数ではですね。

こういったリストとかDICTIONARYとかですね。

参照形のものをデフォルトとして置いておくとですね。

バグに繋がる可能性があるということはですね。

覚えておいていただければと思います。

はいそれではですね。

これPYTHONでどう解決するかなんですけども、やっぱりプログラム解説はですね。

毎回毎回空のリストをですね。

デフォルトとして使いたいっていう場合ありますよねで、その時はにはですね。

こちらをですね。

空のリストでデフォルト引数で渡すんではなくて、NONとしておくんですねで、このプログラムの中で毎回毎回リストをですね。

初期化するんですが、その場合にはですね。

もしIFAIRがもしのんであればですね。

こちらのAIRに空のリストをですね。

初期化して入れてやるという風にします。

そうすればですね。

引数として何かが渡されてきた場合ですね。

渡されてきた場合には何かが入ってるのでそのままですね。

アペンドしてやるとなのでこちらのですね。

コメントアウトした時には別にですね。

問題はないんですけどもで、AIRをですね。

空のリストでですね。

デフォルト引数として使えないので、この場合には何も引数として渡されなかった場合の音が入ってますので、NONの時だけ。

このプログラムの中で自分でですね。

この空のリストを初期化して渡してやるという。

風にやればいい訳ですで、これ実際実行するとですね。

右手に100が入ったリストが返ってくるという風になってます。

なのでちょっと中身的にですね。

ちょっと難しくて分かりにくいかと。

思うので、PYTHONではですね。

文字列とか整数とかそういったものはですね。

デフォルト引数として渡していいんですけども、空のですね。

リストとかディクショナリとかをデフォルト引数を用いる場合には、こちらのを用いて関数内の一番元の部分ですね。

こちらでですね。

自分でですね。

カラーのリストを作ってやるとというようにですね。

今は覚えといていただければいいのかなと思います。