あれ、次はですね。

インポートエラーというものをやっていきたいと思います。

ちょっとこの辺はですね。

コメントアウト全部していきますねで例えばですね。

これをですね。

フロムのレッスンパッケージのツールズのインポートのいうテイルズとやっていうテイルズの生徒はリストこのようにですね。

使ってた訳ですで、これはですね。

今、私が実行しているディレクトリーは、こちらのPYTHONアンダースコアをプログラミングのホールダーの中ですねで、その同じディレクトリーにある。

このローカルのレッスンアンダースコアパッケージを一番初めにPYTHONを探しに行くので、こちらのフロムと帰ったらですね。

まず、自分のこちらの同じディレクトリにあるパッケージ内を探すわけです。

で、これは私以前ですね。

このように書いてましたね。

フロムレッスンパッケージインポートYOUテイルズ要は、このYOUFUSEを移動したんですけども。

移動する前はこのように言うDIR使ってたんですね。

なのでもし私がですね。

コードを移動したら、下のコードは実行できない訳です。

で、これどうするかなんですけども、トライでエックスセプトでですね。

インポートエラーというのをキャッチしてやるんですね。

で、このように書いてやりますで、これで実行すればですね。

何もエラーが出ないということになりますで、これはですね。

こちらのインポートが成功すればですね。

そのまま次に行くんですけども、これはもちろんビューテイルズ移動したので、インポートエラーが出て、次のインポートが成功して次のよう

に言ってですね。

プログラムが正常に動いたという風になりますでよくですね。

パッケージ提供する時にバージョンが付いてるんですけども。

例えば古いバージョンの時はこれで新しいバージョンのですね。

パッケージの場合はこちらという場合にですね。

このような書き方しとけばいいんですね。

なのでこのレッスンドットピッグが古いパッケージでも新しいパッケージでもどちらでもいいようにするという。

風な時にですね。

こういったトライエックスとインポートエラーなんで使えますので、覚えておいていただければと思います。