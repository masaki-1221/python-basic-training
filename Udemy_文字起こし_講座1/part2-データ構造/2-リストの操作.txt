はい、それではですね。

リストの操作をやってみたいんですけども、例えばこちらの変数ESにですね。

アルファベットをちょっと入れていこうかと思いますで、ABCにEFという風にですね。

入れてみます。

で、これ出力するとええから中まで入ってますねでこれ。

ESのですね。

1万18が入ってますね。

で、これを書きかえる場合なんですけども、これそのままですね。

ACEの0にデータを入れてやっても大丈夫です。

これ文字列の時にですね。

やろうとしたらエラー出てたかと思うんですけども、配列の場合はですね。

書き換え可能ですので、このようにESの0番目にエックスを入れてやってESを表示すると一番初めが。

エックスになってるのがお分かりかと思いますでESのですね。

2番目から5番目とやりましたね。

これスライスでですね。

表示するとCDEと入ってるんですけども、これを書き替えたい場合はですね。

これ配列にですね。

また、このように3つ配列を作って入れてあれば、そのまま書き替えてくれるんですね。

で、これ実行してESを押すとですね。

こちらの小文字のCDEがKAPITALの大文字になってるのがお分かりかと思いますで、このESのですね。

2から5番目、こちらスライスをですね。

全て空にしたい場合、このままですね。

空のリストを入れてやればいいんですね。

で、これでやって実行してESを見てみると、CDEが消えてるのがお分かりになるかと思いますで、ESのですね。

1と最後を省略すると、これ全体を指すということだったんですけども、これに体の1層入れてやってもですね。

これESがですね。

全て空になるということになってますで、次はですね。

リストのメソッドを使って色々操作できるんですけども、例えば分かりやすいようにですね。

このように7890と目数字をですね。

入れてみますで、これの中身は今このようになってますね。

で、これ最後にですね。

データを付け加えたい場合に、このようにアペンドとやるんですねで、例えば100という風に入れてやるんですけども、これやるとですね。

一番最後に100が追加されてますで、一番初めに入れた場合をどうするかなんですけども、これ一番元のインデックスが0なので、0という

ふうに指定するんですねでインサートというメソッドがあるので、これで200という風にやるとですね。

これで実行すると、一番初めに200が入ってるということがお分かりかと思います。

で今ですね。

一番最後に言うた。

この100個をですね。

また、取り出したいという時にはですね。

POPというものがあるんですねで、これ後100が返ってきてですね。

もう一度見ると100が取り除かれてるのがお分かりかと思います。

で、一番先頭を取り出したい時はですね。

このインデックス。

また指定してやってですね。

0という風にやるんですね。

そうすると200が返ってきて。

エヌの状態は200がなくなっているということができるようになってます。

で削除したい場合なんですけども、ちょっと出る文というのがあってですね。

これで得るの0番目とやるとですね。

これ一番元の1が消えるんですね。

で、これやってみると。

はい1が消えてるのがお分かりかと思いますで、この出る結構強力でですね。

例えば間違ってこれとやってしまいますね。

そうすると。

EN自体の存在がなくなってしまいますので、こういうでやるとですね。

これは定義されてないことになってしまって、エラーが出るんですね。

なので気を付けて使っていただければと思います。

なので例えばですね。

もう一度。

ENを定義するんですけども、12223という風にやりますねで、これでリムーブというものがあるんですね。

で、これで例えば2という風にやってみます。

そうすると一番初めの2をですね。

消してくれるんですね。

で、これで犬を見ると1223となってます。

でこれもう一度ですね。

削除しますでもう一度削除すると、もう今の状態ないですね。

で、これでちょっともう一回やってみるとどうなるかなんですけども、これあえてですね。

エラーが返ってくるようになってます。

このエラーはですね。

あと、エラーハンドリングとかやるんですけども、もうリムーブできるものがないですよということをですね。

プログラマーに教えてくれますので、それに応じてアプリケーションを開発する手助けになるので、このようにですね。

VALUEERRORというものが返ってくるようになってるんですね。

はいで次なんですけども、次はですね。

ちょっとリストの結合をやってみますね。

例えば、1から5までに入れてですね。

2ですね。

678890という風に入れてみたいと思いますで、これがこのような状態でビーが、このようにデータが入ってますで、これ新しいリスト

をですね。

作りたい場合にエックスという風に作ってですね。

えーとEARTHとしてやれば、エックスが1から10まで入りますけども、例えばですね。

今の状態だとえーもビーもこのままですよね。

この1のこの2ですね。

のデーターを付け加えたい時にはどうするかなんですけども、これプラスイコールしてですねとやると、こののリスト自体がですね。

1から10までデータが入りますので、これも覚えておいてくださいで、このようにやらなくてもですね。

もう違うやり方あるんですけども、またエックスをですね。

定義しますね。

でこれで。

XYは678890という風に入れてみます。

で、リストにはメソッドがあるんですけども、エックスをしてですね。

ドットでこちらエクステンドという風にやるんですね。

で、これは入れてやるとエックスは拡張されてですね。

エックスに1から10まで入るといったものもありますので、こういったですね。

メソッドを覚えていただければですね。

リストの操作も簡単になってくるかと思います。