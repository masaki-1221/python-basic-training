はい、それではですね。

たっぷりやっていこうと思いますで、タプルはですね。

非常にリストと似てるんですけれども、リストの場合はですね。

こちらのブラケットのかぎ括弧で隠れてたんですけども、タプルの場合はですね。

このエンティスのこの丸括弧で括ってやりますで、これでですね。

1234。

また12なんていう風にですね。

入れてやってみます。

で、これ出力するとですね。

このようにエンティスのかぎ括弧に入ってタプルが表示されてますで、これタイプでですね。

見てみてもタプルと出てくるかと思いますで、リストタプルの違いなんですけども、リストの場合はですね。

こちらTEEの0の1のインデックスに値を代入することができたんですけども、タプルの場合にですね。

これ例えば入れてみるとエラーになるんですね。

タプルはそういった新しい値をですね。

アサインするのにサポートされてませんよというようなエラーなんですけども。

タプルはこういったですね。

値を代入することをサポートしてないんですね。

ただですね。

例えば、インデックスで今0やりましたけども、-1とかですね。

例えばまたスライスみたいなこともですね。

できる訳ですで、今TEEがこの状態なんですけども。

例えばですね。

メソッドとしてもTEEのインデックス1がですね。

どこにありますかということで0番目にあるので、1とで次から探す場合にはですね。

次のインデックスから見てほしいのに、4番目に1がまたありますよというようなこういったメソッドがあったりとか、後はですね。

カウントで1が何個ありますかということで、これ今2個あるので2という風に表示するこういったですね。

メソッドもあります。

でヘルプでですね。

例えばリストを見てみるとですね。

リストの場合にはですね。

こちらにアペンドとかですね。

例えばインサートとかリムーブとかこういったリストのですね。

操作を可能とするようなメソッドがあったんですけども、ちょっとですね。

タプルを見てみると、ヘルプでタプルを見てみますねで、これでタプルを見てみるとですね。

カウントとインデックス、これぐらいしかない訳なんですねですね。

値をですね。

入れたらあとはですね。

読み込むような形の時に使うケースが多いということになります。

なのでデータ操作というよりもですね。

値を代入して後、うち、そのデーターの中身を書き替えられたくない時とかですね。

読み込み用の専用みたいな形でですね。

使うケースが多いんじゃないかなと思います。

で、タプルはですね。

値を変更できないと言ったんですけども、このタプルにですね。

リストを入れることができるんですね。

例えばこのようにですね。

タプル内にですね。

他のリストをこのようにネストで入れてみますで、これTEEで出力するとこのような形になるんですけど、勿論このTEEの0はですね。

12123が入ってるんで、これを書き換えようとするとですね。

エラーが出るわけですね。

ただし、これTEEの0の例えば0ですね。

そうするとこれタプルの中の0のインデックスのこの0番目というと、このリストの中の1を指しますよね。

でこれ出力してみると1と出てきます。

これに対して、例えば値を入れるようする場合はですね。

大丈夫なんですね。

はいでタプルの宣言なんですけど、私このようにプレンティスのかぎ括弧でこのように括って宣言したんですけども、これをですね。

外側のこのかぎ括弧をなくしてこのようにですね。

宣言してもタプルになるんですねでこれタイプで。

TEEを見てみてもタプルになってるかと思いますで、これTEE出力するとですね。

パラリンピックのかぎ括弧が付いて出力されてるかと思います。

なのでタプルはですね。

このカンマをですね。

付けた時点で、これタプルという意味になるんですねでちょっとですね。

分かりにくいかもしれないんですけども、これで例えば1しか入れないでですね。

代入して実行してもこれタイプでこの型を調べてみるとタプルと出てくるんですねでこれ。

TEEと出力しても、このように1が入ったタプルになってますで、タプルの場合はですね。

これ1個しか入ってなくても、カンマが付いてですね。

これタプルですよということで表現されます。

なのでこれですね。

結構バグの原因にもなるんですね。

例えばTEEイコール1でですね。

やってみると、これタイプで見ると勿論ですね。

これインテージAが入ってるんですね。

ですが間違ってですね。

プログラムを書いてる最初にこちらのようにですね。

カンマを入れてしまうということがあるとですね。

これタプルになってしまうので、この辺はですね。

バグにならないように気を付けてください。

ではですね。

TEEイコールこちらのように書いてみますねからのタプルをやってやると勿論ですね。

これタイプで見るとタプルなんですね。

で、これで出力してもタプルになってますよね。

ただしですね。

この中に数字の1を入れてカンマなしでやってみるとどうなるかなんですけども、これやってしまうとですね。

TEEは1となってタイプはですね。

インテJARとなってしまうんですね。

なのでこれ気をつけてくださいで、これ勿論ですね。

数字じゃなくて例えばですね。

文字列でも一緒ですね。

文字列でこのように入れてしまうとTEEはですね。

タプルではなくて、文字列として扱われてしまいます。

なので最終的にですね。

1個しか入れないタプルって殆どないとは思うんですけども、カンマをこのように入れてやらないと駄目なんですね。

で、これで出力してやればですね。

タプル型という区分になってます。

でよくあるケースなんですけども、こういったTEEにですね。

例えば、数字の1を入れようと思ってたんですけど、カンマがあってですね。

このTEEに100を出そうという計算をですね。

どこか他の後の方のプログラムを実行するんですけども、こちらの一番初めのTEEがですね。

これがもうタプルになってるタプルに追加で100を数字を入れるということはできないんですね。

なので、これちょっと気をつけてください。

タプルなんですけども、追加とか変更ができないって言ったんですけども、新しいタプルを宣言すれば大丈夫ですね。

例えばですね。

入タプルにしてですね。

123というタプルと456ということをですね。

このタプル同士を繋げて新しいタプルを作るこういったケースにはですね。

新しいタプルがこのように表現されますので、足すことはできます。

それで今ですね。

タプルをですね。

このようにタプル同士をですね。

足してみた訳なんですけども。

例えばですね。

元のタプルをですね。

これ行凸だけやりたいといった場合、間違ってこのようにやってしまうと一番1。

これインテリアになるのでインテリアとタプル型はですね。

足せませんよという風になります。

はいでこれも先程やったようにですね。

こちらのカンマが必要なので、これを忘れないようにしてください。

こうすればですね。

入タプルが1456という風に出されるので、タプルはカンマを付けるということをですね。

忘れないように気を付けてください。