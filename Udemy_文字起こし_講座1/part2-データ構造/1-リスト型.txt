はい、それではですね。

リストをやっていきたいんですけども、今回もまたターミナル使えますね。

はいでこれをですね。

上に引っ張っていただいて、PYTHONと言っても対話型シェルになるかと思いますで、まず初めにですね。

リストなんですけども、こちらのブラケットで囲んでですね。

数字を幾つか、このように入れられるんですね。

要はリスト型っていうのはこのような数字とかですね。

文字列も入れるんですけども、こういった数字は文字列のデーターをですね。

このように配列の中に入れられるんですね。

これENTERをしているを押してみるとですね。

このように数字が入ってますで、例えばこれもですね。

インデックスがありますので、例えばこれ1をですね。

0として出力してみると、1が入って次はですね。

2番目の20が入ってるという。

風になってます。

これ文字列でもやったかと思うんですで、一番最後のデータの出力したい場合ですが、-1にするとこちらの2というものがですね。

返ってきますねでこれをですね。

-2とやると、最後から数えてこちらの2番目のものですね。

こちらが出力されるという風になってます。

後はですね。

文字列のとこでもやったんですけども、例えば0番目から2番目という風に出力するにはこのようにしてですね。

あとはこの0が省略できるということを言いましたよねで後はですね。

例えば2番目から5番目までやってみると、このように表示されますで、例えば一番最後までですね。

見せたい場合にはこれ最後省略してやるとこちらのように出力されますで、1と最後をですね。

省略すると全てのリストが返ってくるという。

風になってますで、このリストもですね。

レングス使えるんですけども、このようにしてやるとですね。

配列の中に幾つデーターが入ってるのかということがですね。

見れますでTYPEでですね。

こちらのAIRを見てみると、リストということでリストがあったということが認識できるかと思います。

後はですね。

文字列からですね。

リストに変換するということはほとんどないとは思うんですけども。

例えばですね。

ABCDBFという風にこのようにですね。

文字を入れてこれをですね。

リストに変換するとどうなるかなんですけども、このようにですね。

1文字1文字取り出してリストに変換してくれますで、今のエルの状態がこのようになってるんですけども、これ例えば100番目ってないですよ

ねでこれをやってみるとどうなるかなんですけども、INDEXエラーが返ってくるということになってます。

で次にですね。

例えばちょっと分かりやすいようにですね。

12345678890という風に入れてみますね。

で、こちらは今犬の状態がこのようになってるんですけども、これをですね。

2つ飛ばしでですね。

摘出したいという時にはどうするかなんですけども、もうこういったように最後にですね。

2を置いてやると、これ一つ飛ばしで13579という風にですね。

摘出しますで、これもちょっとですね。

あまり使わないかもしれないんですけども、これ-1とやるとですね。

後から1987654321という風にですね。

摘出してくれるようなこともあるので、あんまり使わないかもしれないんですけども、ちょっと覚えておいていただければと思います。

はいで次はですね。

ネストさせた配列はどうなるかなんですけども、例えばこれにですね。

ABCという文字列を入れてですねで、エルの方にですね。

123と入れてみます。

で、これ新しい配列を作るんですけども、こちらのとですね。

えのを入れると、これどうなるかなんですけども、これエックス出力するとですね。

このようにネストと言うんですけども、リストの中にまたリストが入ってるというようなリストもですね。

作成可能ですでこれ。

エックス数の0とやるとどうなるかなんですけども、これ1に入ってるABCこちらのデーターが出力されますで、これをですね。

1にしてやると後の123が出力されますねで、こちらのABCのを取るにはどうするかなんですけども、エーの0のですね。

INDEXが1の部分ですねで、これを出力してやるとビーということです。

で、こちらの一番最後にするにはですね。

エックスの1ですね。

それの2番目のインデックスですから、01にですね。

こちらをやると3というのがですね。

出力されるという風になってます。