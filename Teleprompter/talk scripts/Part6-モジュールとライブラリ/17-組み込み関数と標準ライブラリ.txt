次は「組み込み関数と標準ライブラリ」について説明します。

--------------------
■ 組み込み関数
--------------------

組み込み関数とは、importなしで使える関数のことです。
これまでに使った print、len、type、int、str、range なども組み込み関数です。

よく使う組み込み関数を紹介します。

まず、len関数です。
リストや文字列の長さを返します。

（操作：セルを実行）

次に、max関数とmin関数です。
最大値と最小値を返します。

（操作：セルを実行）

次に、sum関数です。
リストの合計値を返します。

（操作：セルを実行）

次に、sorted関数です。
リストを並び替えた新しいリストを返します。
元のリストは変更されません。

（操作：セルを実行）

次に、abs関数です。
絶対値を返します。

（操作：セルを実行）

次に、round関数です。
数値を四捨五入します。

（操作：セルを実行）

--------------------
■ 標準ライブラリ
--------------------

標準ライブラリとは、Pythonに最初から付属しているライブラリです。
pip installなしで使えますが、importは必要です。

よく使う標準ライブラリを紹介します。

まず、datetimeです。
日付や時刻を扱います。

（操作：セルを実行）

次に、osです。
ファイルパスの操作やフォルダの作成などに使います。

（操作：セルを実行）

次に、jsonです。
JSONデータの読み書きに使います。
APIのレスポンス処理でよく使われます。

（操作：セルを実行）

次に、timeです。
処理を一時停止させたり、時間を計測したりできます。

（操作：セルを実行）

次に、reです。
正規表現を使った文字列の検索や置換ができます。

（操作：セルを実行）

これらは生成AIのコードでも頻繁に登場します。

以上が「組み込み関数と標準ライブラリ」の説明でした。
