文字列を結合することも可能でして、プリントの中にこんにちは、○○ですと文字の名前を入れていただいて、文字列を結合するにはですね、イメージ通りかもしれませんが、プラス記号で2つの文字列をつないであげると、1つの文字列として結合してあげることができます。これで実行すると、こんな風にですね、1つの「こんにちは、安井です」という文字列として結合できました。

あとですね、同じように演算記号を使う文字列の扱い方として、掛け算ですね、アスタリスクも使うことができます。「こんにちは * 2」とするとどうなるかというと、こんな風にですね、その掛け算した文字列をこうやって繰り返して結合してくれます。あんまり使うことはないかもしれませんね。

文字列の連結についてはよく使うと思いますので、覚えておいてください。

ここから文字列の概念の中でかなり大事なポイントになってくるんですけれども、文字列の中に数字が入っている場合、どう使われるのかという話になります。例えば、`print`と書いていただいて、ダブルクォーテーションの中に「3」と入れて実行してみてください。そうすると、「3」といつも通りプリントされました。

一方ですね、ダブルクォーテーションに囲まずに3と入力して実行すると、これも同じように出力は3という表示が出ました。この違い何なんだという話ですね。この違いを分かるためには、こんな風に試してみてください。`print`でダブルクォーテーションに囲んだ「3」と普通の3を足し算して実行してみると、こんな風になります。`TypeError`と出ました。「Can only concatenate str」というエラーが表示されます。この`str`というのは文字列を意味します。

また、`int`は整数を意味します。`int`と`str`はよく出てくるので覚えておいてください。これは何を言っているかというと、文字列は文字列としか結合できませんよ、というエラーをPythonは言っているわけです。

さっき`print`してあげると全く同じ表示で「3」と出てきましたし、人間の目で見ても同じ「3」なんですけれども、Pythonから見ると全く違うものに見えているんですね。この「3」と「3」というのは同じ「3」でも違うタイプであるということになります。そのタイプ、型というふうに呼んだりもしますが、それを調べる関数もありまして、その名の通り`type`と打っていただいてください。

では、この文字列としての「3」、ダブルクォーテーションで囲んだ「3」のタイプを調べてみると、`str`（string、文字列型）というふうに表示されます。一方、囲っていない普通の「3」のタイプを調べると、`int`（integer、整数型）というふうに表示されます。`str`はストリング型、`int`はインテジャー型と訳されます。

こんなふうに、人間の目からは同じ「3」に見えても、プログラム上では全く別物だというふうに解釈されているわけです。

というところが非常に大事な概念なので覚えておいてください。この`str`と`int`は相互に変換をすることができます。例えば、先ほどのうまくいかなかった文字列の「3」と数値の「3」。これを何とか普通に3たす3として計算したい場合は、この文字列の方を数値に変えてあげればいいんですね。

そのための関数というのもPythonには備わっていまして、`int`という関数を使います。`int`で括弧の中に文字列で書かれた数値を入れてあげると、こんなふうに文字列としての「3」を数値の「3」に変えてくれるわけです。この変換によって、例えば3たす3の計算を正しく行うことができるようになります。

同じようにですね、整数型の数値というのを文字列に変えるという逆の変換もできます。これは`str`という関数を使います。こうするとですね、結果はどうなるでしょうか。文字列の「3」と文字列の「3」を足し算しているので、最初の方にやりましたが、こういうふうに結合されるんですね。

ちょうど先ほどここでやりましたね。文字列の足し算は結合になると。これがここでも起こっています。「3」と「3」が結合されて「33」になりました。こんなふうにですね、文字列は文字列、数値は数値としての扱い方があるということです。

こんなふうにですね、文字列は文字列、数値は数値としての扱い方があるというところを押さえておいてください。以上、文字列について勉強しました。