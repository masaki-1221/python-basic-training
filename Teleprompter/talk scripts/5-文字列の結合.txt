次は「文字列の結合」です。
これまで計算に使っていた「プラス記号（+）」ですが、
文字列に対して使うと、合体させることができます。
この Hello という文字列と World という文字列、
これをプラスでくっつけてあげると
（操作：セルを実行）
こんな感じで、Hello World とつながって表示されました。

次に、変数と文字をくっつけます。
name という変数に "Mike" という文字列を入れてあげて、
こんな感じでくっつけてあげると、
（操作：セルを実行）
My name is Mike と表示されます。

ただ、この変数と文字をつなぐ書き方に関して、
実務では f-strings という書き方をすることが多いです。

文字列の先頭に「f」を付けて、
変数を波括弧 { } で囲むだけです。
（操作：セルを実行）
例えば変数が多くなった場合にf-stringsを使わないと
こんな感じで「+」やクォーテーションを
何度も書く必要があって面倒です。
f-string と使ってあげると直感的に簡単に書くことができます。
実際AIが書いてくるコードも
f-strings を使ってると思います。

文字列の連結については以上です。