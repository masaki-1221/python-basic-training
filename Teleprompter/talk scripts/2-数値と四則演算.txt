次は「数値」と四則演算についてです。
これもワークデイの基礎研修でやったかもしれませんが、
Pythonでは数値も扱うことができます。

数値の特徴として、文字列と違って「クォーテーションで囲む必要がない」です。
（操作：セルを実行）
こんな感じで数字の 3 が出力されました。

じゃあ四則演算をやってみます。
まずは足し算ですね。
これはもう説明するまでもないかもしれないんですが、
3+7というふうに、プラス記号を使うと足し算をすることができます。
（操作：セルを実行）

ちなみに、間違えて全角を使っちゃうとプログラムは動きません。
コメント文とか文字列、つまりクオーテーションの中身ですね。
これは全角でもいいんですけど、それ以外の
プログラムは基本的に半角で書くと覚えておいてください。

次に引き算ですね。
引き算はマイナス記号を使います。

次に掛け算ですね。
掛け算はアスタリスクを使います。

次に割り算ですね。
割り算はスラッシュを使います。

次に、「%」っていうものを使うと
割り算の余りを求めることができます。

次にスラッシュを2つ書きます。
これは割り算の商を求める、
あるいは小数点以下を切り捨てると考えてください。
マイナスの値を扱う場合はこの説明と違う結果になるんですけど、
実務ではプラスの値しか扱わないと思うんで、
割り算の商を求める、
あるいは小数点以下を切り捨てると思ってもらって大丈夫です。

ちなみに、この計算をクオーテーションで囲むとどうなるかというと、
（操作：セルを実行）
はい、この通り「3 + 7」がそのまま表示されてます。
ということで、数字をクォーテーションで囲んでしまうと
文字列として扱われるという事を覚えておいてください。

数値についての説明は以上です。
